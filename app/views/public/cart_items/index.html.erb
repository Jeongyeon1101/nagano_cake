<h2>ショッピングカート</h2>
<%= link_to "カートを空にする", cart_items_destroy_all_path, method: :delete, "data-confirm" => "カート内の商品をすべて削除します。よろしいですか？" %>
<table>
    <thead>
        <th>商品名</th><th>単価(税込)</th><th>数量</th><th>小計</th><th></th>
    </thead>
    <tbody>
    <% @cart_items.each do |cart_item| %>
        <tr>
          <td><%= image_tag cart_item.item.get_image(70,50) %> <%= cart_item.item.name %></td>
          <td><%= cart_item.item.with_tax_price %></td>
          <td><%= form_with model: cart_item, url: cart_item_path(cart_item.id) do |f| %>
                <%= f.select :amount, *[1..10] %>
                <%= f.hidden_field :item_id, :value => cart_item.item.id %>
                <%= f.submit "変更" %>
              <% end %>
          </td>
          <td><%= cart_item.subtotal %></td>
          <% @total_payment += cart_item.subtotal %>
          <td><%= link_to "削除", cart_item_path(cart_item.id), method: :delete, "data-confirm" => "選択された商品を削除します。よろしいですか？"  %> </td>
        </tr>
    <% end %>
    <tr>
    <td><%= link_to "買い物を続ける", items_path %></td>
    <td>合計金額</td>
    <td><%= @total_payment %></td>
    </tr>
    </tbody>
</table>
<%= link_to "情報入力に進む", new_order_path %>